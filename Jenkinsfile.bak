pipeline {
	agent	{
		label 'ISTHARA-AGENT'
	}
	stages{
		stage('git clone'){
			steps{
				echo 'Git Repo cloned'
			}
		}
		stage('build'){
			steps{
				echo 'this is a Build stage'
			}
		}
		stage('Test'){
			steps{
				echo 'this is Test stage'
			}
		}
		stage('Deploy'){
			steps{
				echo 'this is Deploy stage'
			}
		
		}
	}
}